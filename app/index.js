let ticketLimit = 200;
ticketLimit += 200;
console.log(ticketLimit);

const emails= [
    'frodo@localhost.com',
    'samwise@localhost.com',
    'merry@localhost.com'
];
emails.push('pippin@localhost.com');
console.log(emails);

function hello() {
    let message = 'Hello!';
    console.log(message);
}

function greeting() {
    let message = 'How are you?';
    console.log(message);
}

hello();
greeting();